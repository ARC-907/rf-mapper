# Architecture Overview

This project uses a small MiDaS ONNX model for depth estimation. By default the
weight is expected at `model_small.onnx` in the repository root. Set
`ONYX_MODEL_PATH` to override this location.
