# Development lock for RF Mapper
# Regenerated 2026-07-16 against Python 3.12 (the verified working environment;
# the previous lock pinned numpy 1.25, which has no Python 3.12 wheels and
# broke every installer that consumed this file).
numpy==1.26.4
matplotlib==3.10.8
Pillow==11.3.0
rasterio==1.4.4
onnxruntime==1.27.0; sys_platform != "emscripten" and platform_machine != "wasm32"
scikit-image==0.26.0
opencv-python==4.11.0.86
numba==0.66.0
psutil==7.2.2
itur==0.4.0
whitebox==2.3.6

# Development and build tooling
coverage==7.15.2
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.15.22
pyinstaller==6.21.0
pip-tools==7.5.3

# Optional setup helper dependencies
python-dotenv==1.2.2
colorama==0.4.6
